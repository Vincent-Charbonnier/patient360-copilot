apiVersion: v1
kind: Service
metadata:
  name: {{ include "patient360-copilot.fullname" . }}
  labels:
    {{- include "patient360-copilot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "patient360-copilot.selectorLabels" . | nindent 4 }}
  ports:
    - name: streamlit
      protocol: TCP
      port: {{ .Values.service.streamlitPort }}
      targetPort: streamlit
    - name: api
      protocol: TCP
      port: {{ .Values.service.apiPort }}
      targetPort: api
